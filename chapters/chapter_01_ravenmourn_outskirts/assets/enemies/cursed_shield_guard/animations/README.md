# Animation binding

Runtime binding: `res://shared/resources/enemies/cursed_shield_guard_sprite_frames.tres`, plus shield/effect SpriteFrames in the same shared resource directory.
All bound textures resolve to this Chapter I role's `sprites/` and `effects/` folders.
